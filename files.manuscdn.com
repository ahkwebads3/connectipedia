# تعليمات تثبيت وتشغيل تطبيق تحليل الجمهور المستهدف - Amr Galal

## 📦 الملفات المطلوبة

1. **amr-galal-marketing-tool-source.tar.gz** (1.7 MB) - الكود المصدري الكامل
2. **amr-galal-marketing-tool-dependencies.tar.gz** (92 KB) - ملفات المكتبات

## 🚀 خطوات التثبيت

### 1. فك الضغط

```bash
# فك الكود المصدري
tar -xzf amr-galal-marketing-tool-source.tar.gz
cd amr-galal-marketing-tool-clean

# فك ملفات المكتبات (اختياري - للتحديث)
tar -xzf ../amr-galal-marketing-tool-dependencies.tar.gz
```

### 2. تثبيت المكتبات

```bash
# تثبيت pnpm إذا لم يكن مثبتاً
npm install -g pnpm

# تثبيت جميع المكتبات
pnpm install
```

### 3. إعداد متغيرات البيئة

أنشئ ملف `.env` في جذر المشروع بالمحتوى التالي:

```env
# قاعدة البيانات
DATABASE_URL=mysql://username:password@localhost:3306/amr_galal_db

# المصادقة
JWT_SECRET=your_super_secret_key_here_change_this

# Manus OAuth
VITE_APP_ID=your_app_id
OAUTH_SERVER_URL=https://api.manus.im
VITE_OAUTH_PORTAL_URL=https://portal.manus.im
OWNER_OPEN_ID=your_owner_id
OWNER_NAME=عمرو جلال

# Manus APIs
BUILT_IN_FORGE_API_URL=https://api.manus.im
BUILT_IN_FORGE_API_KEY=your_api_key
VITE_FRONTEND_FORGE_API_KEY=your_frontend_key
VITE_FRONTEND_FORGE_API_URL=https://api.manus.im

# التطبيق
VITE_APP_TITLE=Amr Galal's Ideal Audience Analyzer
VITE_APP_LOGO=/amr-galal-avatar.jpg
VITE_ANALYTICS_ENDPOINT=https://analytics.manus.im
VITE_ANALYTICS_WEBSITE_ID=your_website_id
```

### 4. إعداد قاعدة البيانات

```bash
# دفع التغييرات إلى قاعدة البيانات
pnpm db:push
```

### 5. تشغيل التطبيق

```bash
# وضع التطوير (مع Hot Reload)
pnpm dev

# وضع الإنتاج
pnpm build
pnpm start
```

الآن يمكنك الوصول للتطبيق على:
```
http://localhost:3000
```

## 📋 المتطلبات

- **Node.js**: الإصدار 18 أو أحدث
- **pnpm**: مدير المكتبات
- **MySQL**: قاعدة البيانات
- **مفاتيح Manus API**: للمصادقة والذكاء الاصطناعي

## 🔧 الأوامر المتاحة

```bash
# تطوير
pnpm dev              # تشغيل في وضع التطوير

# الإنتاج
pnpm build            # بناء التطبيق
pnpm start            # تشغيل الإصدار المبني

# قاعدة البيانات
pnpm db:push          # دفع التغييرات إلى قاعدة البيانات
pnpm db:studio        # فتح واجهة إدارة قاعدة البيانات

# الفحص
pnpm lint             # فحص الكود
pnpm type-check       # فحص أنواع TypeScript
```

## 📁 هيكل المشروع

```
amr-galal-marketing-tool-clean/
├── client/                 # الواجهة الأمامية (React)
│   ├── src/
│   │   ├── pages/         # صفحات التطبيق
│   │   ├── components/    # المكونات المعاد استخدامها
│   │   ├── lib/           # المكتبات والأدوات
│   │   └── App.tsx        # التطبيق الرئيسي
│   └── public/            # الملفات الثابتة
├── server/                # الخادم الخلفي (Express + tRPC)
│   ├── routers/          # مسارات tRPC
│   ├── db.ts             # مساعدات قاعدة البيانات
│   └── routers.ts        # جميع المسارات
├── drizzle/              # مخطط قاعدة البيانات
│   └── schema.ts         # تعريف الجداول
├── package.json          # المكتبات والمشاريع
└── pnpm-lock.yaml        # قفل الإصدارات

```

## 🎯 الميزات الرئيسية

✅ واجهة تفاعلية بالعربية  
✅ 7 أسئلة ذكية عن المنتج والجمهور  
✅ تحليل AI متقدم باستخدام Gemini  
✅ شخصيات مشتري مفصلة  
✅ توصيات استراتيجية  
✅ تصدير PDF  
✅ نسخ البيانات إلى الحافظة  
✅ مصادقة آمنة  
✅ تخزين النتائج في قاعدة البيانات  

## 🐛 استكشاف الأخطاء

### المشكلة: لا يمكن الاتصال بقاعدة البيانات
**الحل**: تأكد من أن MySQL يعمل و DATABASE_URL صحيح

### المشكلة: خطأ في المصادقة
**الحل**: تحقق من مفاتيح Manus API والإعدادات

### المشكلة: بطء في التطبيق
**الحل**: تأكد من أن جميع المكتبات مثبتة بشكل صحيح

## 📞 الدعم

للمزيد من المعلومات، زر موقع عمرو جلال:
https://www.amrgalal.net

---

تم إنشاء هذا التطبيق بواسطة Manus AI
